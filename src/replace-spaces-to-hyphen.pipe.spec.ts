import { ReplaceSpacesToHyphenPipe } from './replace-spaces-to-hyphen.pipe';

describe('ReplaceSpacesToHyphenPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceSpacesToHyphenPipe();
    expect(pipe).toBeTruthy();
  });
});
